function* rootSaga({
  viewModels,
  readModels,
  aggregates,
  subscribeAdapter,
  origin,
  rootPath
}) {
  // viewModels,
  // readModels,
  // aggregates,
  // subscribeAdapter,
  // origin,
  // rootPath
}

export default rootSaga
